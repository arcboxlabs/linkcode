# Note: pnpm 11 reads install settings (nodeLinker, autoInstallPeers,
# strictPeerDependencies, allowBuilds, …) from pnpm-workspace.yaml, not from here.
# Kept for tooling that still consults .npmrc.
enable-pre-post-scripts=true
