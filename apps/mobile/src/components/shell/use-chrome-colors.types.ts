/** Navigation-chrome palette: headers, screen backgrounds, and header controls. */
export interface ChromeColors {
  /** Screen + header background. */
  background: string;
  /** Titles and primary glyphs. */
  title: string;
  /** Interactive tint (back chevron, header actions). */
  tint: string;
  /** Quiet glyphs and secondary text. */
  subtle: string;
}
