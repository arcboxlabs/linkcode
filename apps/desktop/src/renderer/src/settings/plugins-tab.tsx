import { PluginsSettingsPanel } from '@linkcode/workbench';

/** Daemon-backed tool settings, shared with the browser client. */
export function PluginsTab(): React.ReactNode {
  return <PluginsSettingsPanel />;
}
