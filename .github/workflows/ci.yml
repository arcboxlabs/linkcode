name: CI

on:
  push:
    branches:
      - master
      - release/*
  pull_request:
    paths-ignore:
      - '**.md'
  workflow_dispatch:
    inputs:
      release_type:
        type: choice
        options:
          - Dry Run
          - Release

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  NODE_OPTIONS: --max-old-space-size=4096

jobs:
  typescript:
    name: TypeScript
    runs-on: blacksmith-2vcpu-ubuntu-2404

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v6

      - name: Setup Node.js
        uses: actions/setup-node@v7.0.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting and imports
        run: pnpm format:check

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

  rust:
    name: Rust
    runs-on: blacksmith-2vcpu-ubuntu-2404

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2

      - name: Format
        run: cargo fmt --check

      - name: Clippy
        run: cargo clippy --all-targets --locked -- -D warnings

      - name: Test
        run: cargo test --locked

  all-green:
    name: All Green
    runs-on: blacksmith-2vcpu-ubuntu-2404
    needs:
      - typescript
      - rust
    if: always()

    steps:
      - name: Check required jobs
        run: |
          if [ '${{ needs.typescript.result }}' != 'success' ] || [ '${{ needs.rust.result }}' != 'success' ]; then
            exit 1
          fi
