/**
 * @linkcode/ipc — business-free system IPC contracts and Eventa definitions.
 * Used by desktop only; application data must not go through this channel.
 */

export type * from './bridge';
export * from './context';
export * from './events';
